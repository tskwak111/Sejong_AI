# AGENTS.md — 세종 민원이음 저장소 지침

이 파일은 Codex가 이 저장소에서 작업할 때 항상 적용해야 하는 **짧고 강한 공통 규칙**이다. 상세 절차는 링크된 문서를 읽는다. 하위 디렉터리의 `AGENTS.md`가 더 구체적인 규칙을 제공하면 그 규칙을 우선한다.

## 1. 프로젝트 미션

시민의 일상어 질문을 **승인된 공식 행정 지식**과 연결한다. 근거가 있으면 출처와 다음 행동까지 안내하고, 근거가 없거나 개인 조회·법적 판단·지원 범위 밖이면 지어내지 않고 안전하게 폴백한다. 지원 범위 안의 근거 부족 질문만 사람의 작성·승인 절차를 거쳐 ACTIVE KB로 전환한다.

기준 문장:

> **모르면 지어내지 않고, 알면 끝까지 안내한다.**

## 2. 반드시 먼저 읽을 문서와 권위 순서

작업 시작 전 다음 순서로 읽는다.

1. `docs/00_SOURCE_OF_TRUTH.md`
2. `docs/source-of-truth/TEAM_DECISIONS.md`
3. `docs/source-of-truth/PROJECT_PLAN.md`
4. `docs/source-of-truth/RFP_MATRIX.md`
5. 해당 영역의 ADR(`docs/adr/`)
6. 현재 활성 계약(`contracts/`)과 DB 초안(`database/`)
7. 현재 작업의 실행계획과 구현 노트

`legacy/`는 참고용이며 **정답·활성 계약·현재 범위가 아니다.** 충돌 시 legacy를 따르지 않는다.

## 3. 첫 작업과 모호성 처리

복잡하거나 저장소 전체에 영향을 주는 작업에서는 코드부터 쓰지 않는다.

1. 저장소·Git·실행환경·기존 코드·문서 충돌을 조사한다.
2. 누락된 요구와 미지의 영역을 적극적으로 찾는다. 사용자가 질문하지 않았다는 이유로 필요한 설계를 생략하지 않는다.
3. 질문은 `아키텍처/데이터/보안/외부연계/배포/비용`에 미치는 영향이 큰 순서로 묻는다.
4. 이미 결정된 내용은 다시 묻지 않는다. `DECISION_LOG.md`, ADR, source-of-truth를 먼저 확인한다.
5. 인간의 결정이 필요한 질문만 인터뷰하고, 낮은 영향의 구현 세부는 합리적인 기본값을 선택해 기록한다.
6. 블로커가 남아 있으면 구현하지 말고 질문한다. 블로커가 아니면 가정을 명시하고 진행할 수 있다.

질문 형식은 `Q-ID / 왜 필요한가 / 선택지와 장단점 / 추천안 / 답이 없을 때 기본값 / 영향 범위`를 사용하고, 한 번에 7개를 넘기지 않는다.

상세 기준: `docs/11_AMBIGUITY_REGISTER.md`, `.agents/skills/ambiguity-audit/SKILL.md`

## 4. 확정 제품 범위

### P0/P1 — 이번 프로젝트에서 실제 구현

- 실제 페이지: `/`, `/chat`, `/admin`
- 지원 분야: 전입·주민등록, 증명서 발급, 대형폐기물, 지방세 일반 안내
- ACTIVE KB 기반 구조화 답변과 서버 결합 출처 카드
- 모호 질문 FOLLOWUP
- 폴백: `INSUFFICIENT_GROUNDING`, `PERSONAL_LOOKUP`, `LEGAL_JUDGMENT`, `OUT_OF_SCOPE`
- 읍·면·동 직접 선택 기반 공식 기관 카드
- 실패 질문 → 사유 확인 → KB 후보 → 작성자와 다른 승인자 → ACTIVE KB → 재질의 개선
- 외부 LLM 호출 전 개인정보 마스킹, 원문 DB 미저장
- 쉬운 말·큰 글씨·본문 대비 4.5:1 이상·키보드 접근성
- 표본 20개 + 회귀 테스트 1개 + 제한적 100명 스모크 테스트

### P2 — 이번 구현에서 제외

- 실제 GPS·거리 계산·내장 지도
- 실제 신청·상태조회·개인별 세금 조회
- 정부24/내부 업무 시스템 연계
- 다국어·음성
- 자동 클러스터링·급증 탐지·주간 AI 리포트
- 전체 KB CRUD·다기관 SaaS·SSO/RBAC/전자결재

P2를 사용자의 명시적 범위 변경 없이 구현하지 않는다.

## 5. 비협상 안전 규칙

- 질문 원문을 애플리케이션 DB, 액세스 로그, 오류 추적 도구에 저장하지 않는다.
- 개인정보 마스킹은 외부 LLM 호출 전에 수행한다.
- 출처명·URL·확인일은 LLM이 생성하지 않는다. 서버가 KB 메타데이터에서 결합한다.
- 시민 답변 검색 대상은 `ACTIVE` KB만이다.
- `OUT_OF_SCOPE` 질문 텍스트는 저장하지 않는다.
- FOLLOWUP은 실패 질문이 아니며 실패 목록에 저장하지 않는다.
- 작성자가 자기 후보를 승인할 수 없다.
- 감사 로그에 질문·답변 전체 스냅샷을 넣지 않는다.
- 공식 데이터와 mock 데이터를 섞지 않는다. mock에는 `시연용 샘플` 표시를 강제한다.
- 비밀값·키·토큰·실제 개인정보를 커밋하지 않는다.

## 6. 구현 방식

- 큰 작업은 `PLANS.md` 형식의 실행계획을 먼저 만든다.
- 한 번에 하나의 수직 흐름(vertical slice)을 끝낸다.
- 새 프로덕션 의존성, 공개 API 계약 변경, DB 마이그레이션, 개인정보 정책 변경, 배포 아키텍처 변경은 인간 승인을 받는다.
- 내부 리팩터링·테스트 추가·포맷팅·명명 정리는 행동 변화가 없고 검증 가능하면 자율적으로 수행할 수 있다.
- 오래된 스타터 코드에서 코드를 복사할 때는 현재 계약과 정책에 맞는지 먼저 검증한다.
- 가능한 한 타입·스키마·열거형으로 상태를 제한하고 문자열을 산발적으로 하드코딩하지 않는다.
- 협업 권위는 ADR-0019와 승인된 collaboration spec을 따른다. 인간 Frontend 팀원은 허용된
  frontend-only green PR만 자가 병합할 수 있으며 계약·backend·DB·migration·official data·
  privacy/security·dependency/lockfile 변경은 사용자 검토로 승격한다.
- Codex Cloud는 branch와 Draft PR을 만들 수 있지만 스스로 merge하지 않는다. local Codex의
  commit/merge 권한은 현재 task의 명시적 인간 승인 범위만 따른다. GitHub Free에서 merge 버튼이나
  green CI가 보이는 사실을 인간 승인으로 간주하지 않는다.
- Codex Cloud에는 DeepSeek key·DB DSN·context secret을 넣지 않고, Docker/Supabase/DeepSeek actual
  검증은 local-only로 기록한다. private GitHub source remote는 public deployment나 remote DB
  승인이 아니다.

## 7. 테스트와 완료 기준

변경한 영역에 맞는 테스트·린트·타입체크·빌드를 실행한다. 실행할 수 없는 검증은 이유와 대체 증거를 기록한다.

작업은 다음을 모두 만족해야 완료다.

- 요청한 동작과 인수 기준 충족
- 관련 자동/수동 테스트 통과
- 보안·개인정보·데이터 영향 검토
- 계약·문서·버전 매니페스트 동기화
- diff 자체 검토
- 구현 노트 작성 및 `INDEX.md` 갱신

상세: `docs/08_TEST_STRATEGY.md`, `docs/09_DEFINITION_OF_DONE.md`

## 8. 구현 노트 의무

**사용자의 한 요청/질문을 완료할 때마다**, 코드 변경이 없어도 구현·결정 노트를 작성한다.

- 위치: `docs/implementation-notes/`
- 형식: `IMP-YYYYMMDD-NNN-short-slug.md`
- 템플릿: `docs/implementation-notes/TEMPLATE.md`
- 생성 도구: `python scripts/new_implementation_note.py --title "..."`

노트에는 육하원칙, 변경 이유, 파일, 명령, 테스트, 버전 전후, 데이터·보안 영향, 가정, 미해결 사항, 롤백, 인수인계 정보를 포함한다. 처음 보는 사람이 이 노트와 저장소 문서만으로 작업을 재현할 수 있어야 한다.

## 9. 인간이 알아야 하는 것과 AI 내부 세부 구분

모든 계획·노트·최종 답변에서 다음을 나눈다.

- **인간이 알아야 하거나 승인해야 하는 내용**: 범위, 사용자 동작, 비용, 보안/개인정보, 데이터 출처, 공개 계약, DB 마이그레이션, 외부 공급자, 배포, 위험, 트레이드오프.
- **AI가 내부적으로 처리 가능한 내용**: 같은 계약 안의 helper 함수, 파일 분할, 테스트 픽스처, 포맷팅, 비공개 리팩터링, 명명 개선.

상세: `docs/10_HUMAN_AI_BOUNDARY.md`

## 10. 최종 응답 형식

작업 종료 응답에는 다음을 짧게 포함한다.

1. 무엇을 완료했는가
2. 인간이 확인·결정할 것
3. 실행한 테스트와 결과
4. 주요 변경 파일
5. 구현 노트 경로
6. 남은 위험/다음 한 단계

완료하지 못한 사항이나 검증하지 못한 사항을 숨기지 않는다.
