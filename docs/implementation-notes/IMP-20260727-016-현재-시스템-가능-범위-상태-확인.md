# IMP-20260727-016 — 현재 시스템 가능 범위 상태 확인

- Date/Time (KST): 2026-07-27T11:23:20+09:00
- Task ID: STATUS-CAPABILITY-001
- Type: status-assessment
- Status: Done
- Author/Agent: 사용자 제품 결정자 / Codex
- Branch: codex/ACTUAL-P0-UX-GAPS-001
- Base commit: 7526f10
- Related plan/ADR/RFP:
  - `docs/superpowers/specs/2026-07-27-natural-civic-dialogue-and-operations-design.md`
  - `docs/superpowers/plans/2026-07-27-natural-civic-dialogue-and-operations.md`
  - ADR-0010, ADR-0018, ADR-0023~0026
  - PR #18

## 1. 사용자 요청과 완료 기준

### 요청

- 현재 구현으로 실제 어디까지 가능한지 쉽게 설명한다.

### Acceptance Criteria

- 현재 Draft PR과 `main`의 차이를 구분한다.
- 시민·관리자·AI·DB·공개 배포 기능을 실제 증거에 맞춰 구분한다.
- 구현된 기능과 아직 불가능하거나 사람이 구성해야 하는 기능을 과장 없이 기록한다.

## 2. 육하원칙(6W1H)

| 항목 | 기록 |
|---|---|
| Who — 누가 | 사용자 제품 결정자, Codex 상태 감사자 |
| When — 언제 | 2026-07-27 KST, PR #18 생성과 원격 CI 완료 후 |
| Where — 어디서 | private Sejong_AI, feature worktree, local API/Web/PostgreSQL, GitHub Draft PR |
| What — 무엇을 | 현재 실행 가능한 시민·관리자·AI·데이터·배포 범위 |
| Why — 왜 | 병합·데모·다음 개발 우선순위를 혼동하지 않도록 실제 경계를 설명하기 위해 |
| How — 어떻게 | source-of-truth, TASKS, env example, version manifest, PR/CI 상태를 대조 |
| How much — 어느 정도 | 지원 분야 4개, local ACTIVE 20, classifier 60/60, root 33단계, E2E 24 |

## 3. 시작 전 상태

- 관련 파일: `docs/00_SOURCE_OF_TRUTH.md`, `docs/source-of-truth/TEAM_DECISIONS.md`,
  `TASKS.md`, `README.md`, `apps/api/.env.example`, `versions/manifest.json`.
- 기존 동작: feature branch는 자연 대화·관리자 운영·DB·actual evidence까지 구현됐고 PR #18은
  `CLEAN/MERGEABLE`, 원격 검사 전부 PASS다.
- 발견한 충돌/부채: feature는 아직 Draft PR이므로 최신 `main`의 기능이라고 말할 수 없다.
  public application/remote DB target도 구성되지 않았다.
- Git 상태: `codex/ACTUAL-P0-UX-GAPS-001`, HEAD/remote `7526f10`, 조사 시작 시 clean.

## 4. 미지의 영역·가정·인터뷰

| ID | 구분 | 내용 | 결정/기본값 | 영향 |
|---|---|---|---|---|
| A-063 | B / High | public target/account/region/origin/budget/DNS/saved version | target 미구성, 추측하지 않음 | remote migration·seed·deploy·smoke |

## 5. 설계 결정과 대안

### 선택

- 기능을 `PR #18 병합 후 local/private 가능`, `선택적 local AI`, `아직 불가능`으로 나눠 설명한다.
- 현재 GitHub private remote를 public 서비스나 remote DB로 간주하지 않는다.

### 이유

- 코드 존재, local actual 검증, `main` 병합, public 운영은 서로 다른 완료 단계다.

### 고려했지만 선택하지 않은 대안

- “모두 완성”으로 요약: public target·admin auth·기관 연계가 없어 부정확하므로 기각.
- “AI 챗봇 미구현”으로 요약: bounded classifier와 grounded generation actual이 있어 부정확하므로 기각.

## 6. 구현 상세

| 파일/영역 | 변경 내용 | 이유 |
|---|---|---|
| 이 상태 노트 | 실제 가능 범위와 남은 경계 기록 | 인수인계 |
| `INDEX.md` | 상태 노트 행 추가 | 문서 추적 |

### 데이터 흐름/상태 변화

- 제품 코드·DB·계약·공식 데이터·환경 변수·외부 상태 변경 0.

### 오류·빈 상태·롤백

- public target이 없으면 배포를 시도하지 않고 `Not executed: target not configured`로 닫는다.

## 7. 버전 전후

| 축 | Before | After | 변경 이유 |
|---|---|---|---|
| Application | 0.11.0-natural-dialogue | 동일 | 상태 확인만 수행 |
| Web | 0.7.0-natural-dialogue | 동일 | 상태 확인만 수행 |
| API | 4.0.0-draft | 동일 | 상태 확인만 수행 |
| DB schema | 0.5.0-local | 동일 | 상태 확인만 수행 |
| Official data | 0.1.0-initial.2 | 동일 | 데이터 변경 0 |
| Mock data | 0.0.0-not-populated | 동일 | mock 변경 0 |
| Prompt set | 0.3.1-hybrid-classifier | 동일 | prompt 변경 0 |
| Test suite | 1.9.1-natural-dialogue | 동일 | 테스트 변경 0 |
| Docs | 2.25.0 | 동일 | 요청별 상태 노트만 추가 |

## 8. 명령과 테스트 증거

| 명령/검증 | 결과 | 시간/개수 | 증거 경로 |
|---|---|---|---|
| `git status -sb` | 조사 시작 clean | branch 1 | local output |
| `gh pr view 18 --json ...` | Draft, CLEAN, MERGEABLE, 원격 검사 SUCCESS | PR 1 | GitHub PR #18 |
| `rg` authority/status 대조 | 지원 4분야·local ACTIVE 20·provider/public 경계 확인 | authority 6개 | SOT/TASKS/README/env/manifest |

### 미실행 검증과 이유

- 제품 테스트는 직전 final gate의 root 33단계·E2E 24·focus 135와 원격 CI를 재사용했다.
  이번 요청은 읽기 중심 상태 설명이며 제품 코드 변경이 없다.
- remote smoke는 target 미구성이다.

## 9. 보안·개인정보·접근성·성능 영향

- Privacy: 질문 원문·PII·provider payload를 읽거나 기록하지 않았다.
- Security: secret 값은 확인하지 않았고 `.env.example`의 key 이름과 false 기본값만 대조했다.
- Accessibility: 변경 0. 직전 390/430/desktop E2E 증거를 인용한다.
- Performance/cost: 외부 API 호출 0, 비용 0.

## 10. 데이터와 출처 영향

- 공식 데이터: 변경 0. local actual ACTIVE 20 증거만 인용.
- mock/AI 생성: 변경 0.
- schema/lineage: 변경 0.
- verified date: 2026-07-27 KST.

## 11. 인간이 반드시 알아야 하거나 승인할 내용

- PR #18을 병합하기 전에는 이 기능들이 `main`의 기능이 아니다.
- local/private 시민·관리자 MVP는 가능하지만 실제 신청·상태조회·결제·정부24 연계는 아니다.
- public 시민 데모에는 A-063의 배포 대상·DB·origin·DNS·예산·rollback version 구성이 필요하다.
- public 관리자에는 별도의 인증/RBAC 설계와 검증이 필요하다.

## 12. AI 내부 구현 세부 — 필요할 때만 보면 되는 내용

- deterministic safety route, masked ambiguous-only classifier, server-owned source 결합과 signed
  context 구현은 공개 동작 경계 안의 내부 구성이다.

## 13. 인수인계·재현·롤백

### 재현

- PR #18의 통합 report와 final implementation note를 읽고 `scripts/verify.ps1`을 실행한다.

### 롤백

- 상태 노트만 제거하고 INDEX 행을 되돌리면 된다. 제품 rollback은 필요 없다.

### 다음 개발자 시작점

- PR #18을 검토·병합한 뒤 local demo를 실행한다. public을 원하면 A-063을 먼저 결정한다.

## 14. 남은 위험·미해결 질문·다음 단계

- A-063 public infrastructure configuration.
- public admin authentication/RBAC.
- 네 지원 분야 밖 공식 데이터 확장과 실제 기관 API 연계는 별도 범위다.

## 15. 자체 리뷰

- [x] 요청 충족
- [x] 테스트/검증 증거를 재실행과 인용으로 구분
- [x] source-of-truth/계약/버전 영향 확인
- [x] 개인정보 원문 노출 없음
- [x] 구현 노트 INDEX 갱신
