# IMP-20260727-004 — 자연스러운 대화 분류 아키텍처 1부 승인

- Date/Time (KST): 2026-07-27T00:41:42+09:00
- Task ID: `CHAT-NATURAL-DESIGN-SECTION-1`
- Type: decision-design
- Status: Decision-only Done / design section 2 pending
- Author/Agent: 사용자 승인 / Codex 기록
- Branch: `codex/ACTUAL-P0-UX-GAPS-001`
- Base commit: `a721965`
- Related: D-085~D-089, ADR-0010/0023/0024/0025, CHAT-NATURAL-001

## 1. 사용자 요청과 완료 기준

사용자가 CHAT-NATURAL-001 설계 1부를 승인했다. 승인된 요청 처리 순서와 최소 구조화
문맥 경계를 결정 로그와 task에 기록하되 공개 계약·DB·제품 코드는 아직 변경하지 않는다.

## 2. 육하원칙(6W1H)

| 항목 | 기록 |
|---|---|
| Who | 사용자가 설계를 승인하고 Codex가 추적 문서를 갱신 |
| When | 2026-07-27 KST |
| Where | decision log, TASKS, version/changelog/note |
| What | privacy-first hybrid route와 raw transcript 없는 15분 context 승인 |
| Why | 자연스러움과 공식 근거·개인정보 원칙을 동시에 유지 |
| How | D-089로 승인 범위와 아직 미확정인 후속 설계를 분리 |
| How much | product/API/DB/provider/data 변경·actual call 0 |

## 3. 시작 전 상태

- Q-PROD-REAL-001=A로 현실형 공식 안내·운영센터 목표가 확정됐다.
- hybrid classifier 방향과 호출·비용 상한은 Q-CLASS-001/002=A로 확정됐다.
- 상태·공개 응답·저장·오류·테스트 상세는 아직 설계되지 않았다.
- 시작 HEAD `a721965`, worktree clean.

## 4. 미지의 영역·가정·인터뷰

| ID | 구분 | 내용 | 상태 | 영향 |
|---|---|---|---|---|
| Design 1 | 승인 | privacy→policy→classifier→route→grounding→answer/source | Approved / D-089 | 전체 chat pipeline |
| Design 2 | B / 공개 동작 | route별 응답·저장·context v2 claims | Pending | API/DB/Web/privacy |
| Design 3 | B / 장애·검증 | fallback, latency, acceptance matrix | Pending | runtime/test/cost |

## 5. 설계 결정과 대안

### 선택

- PII redaction 선행
- deterministic policy/high-confidence route
- 안전한 ambiguous 질문만 bounded Upstage classifier
- server가 route·검색·저장·출처를 최종 결정
- ACTIVE/OFFICIAL grounding 뒤 generated/template
- raw transcript/프로필 없이 15분 server-issued structured context

### 제외

- 모든 질문·대화 history를 LLM에 전송
- 모델이 source/storage/candidate eligibility 결정
- KB 없는 분야의 자유 답변

## 6. 변경 상세

| 파일 | 변경 |
|---|---|
| `DECISION_LOG.md` | D-089 |
| `TASKS.md` | section 1 approved / section 2 pending |
| `CHANGELOG.md`, version docs/manifest | documentation `2.22.5` |
| 이 노트와 INDEX | 승인 재현 |

Runtime/data flow는 아직 바뀌지 않는다. 롤백은 이 documentation commit revert다.

## 7. 버전 전후

| 축 | Before | After |
|---|---|---|
| Application | `0.10.0-office-directory-runtime` | 동일 |
| Web | `0.6.0-answer-mode` | 동일 |
| API | `3.3.0-draft` | 동일 |
| DB schema | `0.4.0-local` | 동일 |
| Official data | `0.1.0-initial.2` | 동일 |
| Prompt set | `0.2.0-grounded-live-chat` | 동일 |
| Test suite | `1.8.0-local-demo-readiness` | 동일 |
| Docs | `2.22.4` | `2.22.5` |

## 8. 명령과 테스트 증거

| 검증 | 결과 |
|---|---|
| `git status --short`, `git log -2 --oneline` | 시작 상태 clean, HEAD `a721965` |
| `python -B scripts/check_repository_docs.py` | PASS — `repository documentation check passed` |
| `scripts/check_secret_patterns.ps1 -RepositoryRoot .` | PASS / findings 0 |
| `git diff --check` | PASS |
| `python -m json.tool versions/manifest.json` | PASS |

API/Web/DB tests와 provider/DB actual은 제품·test source를 바꾸지 않은 partial design 승인이라
실행하지 않는다.

## 9. 영향

- Privacy/Security: raw transcript·프로필 미저장과 server authority를 승인했다.
- Accessibility: 변경 없음; 후속 공개 동작 설계 대상.
- Performance/cost: Q-CLASS-002 cap 유지, actual call 0.
- Data/source: record, schema, official/mock data 변경 0.

## 10. 인간이 반드시 알아야 하는 내용

- 이번 승인은 pipeline architecture만 승인한다.
- 새 public enum, scope-gap DB row, context v2 claims, 화면 문구는 아직 승인되지 않았다.
- 설계 2·3과 written specification 승인 뒤에만 코드를 구현한다.

## 11. AI 내부 구현 세부

- helper·enum validator·fixture 분리는 최종 계약 안에서 자율 처리할 수 있다.

## 12. 인수인계·롤백·다음 단계

1. D-089와 ADR-0010/0023/0024/0025를 읽는다.
2. 설계 2에서 route별 공개 응답·저장·context claims를 확정한다.
3. 설계 3에서 오류·latency·test acceptance를 확정한다.
4. 전체 승인 뒤 written spec을 작성한다.

롤백은 이 documentation commit revert이며 DB/data rollback은 없다.

## 13. 자체 리뷰

- [x] 사용자 승인 기록
- [x] final 문서/secret/diff 검증
- [x] 버전·INDEX 동기화
- [x] 시민 질문 원문·비밀 없음
