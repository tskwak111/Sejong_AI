# IMP-20260726-008 — 현재 MVP 진행상황과 다음 개발 순서

- Date/Time (KST): 2026-07-26T14:22:33+09:00
- Task ID: STATUS-20260726
- Type: status-handoff
- Status: Done
- Author/Agent: Codex primary agent
- Branch: codex/LLM-003-metadata-explanation
- Base commit: 9fe9aa1
- Related:
  - [Current status handoff](../handoffs/HANDOFF-20260726-CURRENT-MVP-STATUS.md)
  - [Project plan](../source-of-truth/PROJECT_PLAN.md)
  - [TASKS](../../TASKS.md)
  - [LLM-003 plan](../superpowers/plans/2026-07-25-grounded-live-chat-generation.md)

## 1. 사용자 요청과 완료 기준

### 요청

- 지금 무엇을 해야 하는지
- 지금까지 무엇을 얼마나 만들었는지
- 전체 범위에서 얼마나 남았는지
- 남은 작업을 어떤 순서와 규모로 진행할지 설명한다.

### Acceptance Criteria

- 최신 remote/Git/PR/CI 상태를 확인한다.
- TASKS·최신 계획·test report·actual runtime/DB를 교차 검토한다.
- local MVP, original P0/P1, public readiness를 분리해 진행률을 제시한다.
- 완료 기능·실행 상태·간극·다음 수직 흐름·사람 작업을 분리한다.
- 코드·DB·provider를 변경하지 않고 reproducible handoff를 작성한다.

## 2. 육하원칙(6W1H)

| 항목 | 기록 |
|---|---|
| Who — 누가 | 사용자가 전체 상황을 요청했고 Codex가 current source/runtime evidence를 감사했다. |
| When — 언제 | 2026-07-26 KST, PR #13 병합과 Q-DB-CLEANUP-001=A 확정 뒤 |
| Where — 어디서 | private GitHub, local worktree, Docker local DB, active docs/code/contracts/tests |
| What — 무엇을 | 완료율·실제 기능·실행 상태·contract gap·다음 3개 slice를 정리했다. |
| Why — 왜 | local MVP 완료와 public readiness를 혼동하지 않고 다음 개발 투자 순서를 정하기 위해서다. |
| How — 어떻게 | fetch/GitHub PR·CI 조회, TASKS/plan/report review, FastAPI OpenAPI inventory, read-only DB count와 port probe를 사용했다. |
| How much — 어느 정도 | top-level TASK 39개, Done 27/Partial 3/남은 9; DB ACTIVE 20/office 3/mapping 10; 문서만 변경 |

## 3. 시작 전 상태

- remote main: `be7387f`, PR #13 merged.
- current docs branch: `9fe9aa1`, clean, remote 미게시.
- local 기본 `main=f5c3a1d`는 remote main보다 뒤였다.
- PR #11/#13 MERGED, open PR 0.
- current main hosted Collaboration policy/Frontend CI SUCCESS.
- Docker DB container healthy, API/Web process는 꺼져 있었다.
- version manifest documentation `2.20.3`.

## 4. 조사 결과

### 실제 완료

- 시민 `/`, `/chat`과 local/private 관리자 `/admin` 흐름
- PII→분류→ACTIVE/OFFICIAL 검색→grounding→구조화 답변/폴백
- Upstage optional generated summary와 complete template fallback
- 실패 질문→사유 확정→후보→별도 승인자→20번째 ACTIVE→재질의 SUCCESS
- 19→20 DB/data lineage, durable idempotency, 30일 text purge
- sample 20/20, source 10/10 actual, Web unit/browser와 root offline publication evidence

### 실제 runtime

- Docker `29.2.1`
- `supabase_db_sejong-ai-local` healthy
- read-only SQL: ACTIVE/OFFICIAL KB 20, OFFICIAL office 3, mapping 10
- API `127.0.0.1:8000` not running
- Web `127.0.0.1:3000` not running

### 새로 명확해진 gap

- tracked OpenAPI의 `GET /api/v1/offices`가 default/local FastAPI runtime에 없다.
- tracked `/api/v1/admin/quality-summary`는 200 schema와 runtime route가 없다.
- actual admin UI는 quality summary를 “연동 준비 중”으로 표시한다.
- hosted CI는 Frontend/policy만 있고 Python/API/DB gate는 local evidence다.

## 5. 진행률 판단

| 범위 | 추정 | 판단 방식 |
|---|---:|---|
| local/private 평가·데모 MVP | 약 90% | 대표 기능과 automated/actual gate는 완료, manual a11y/demo와 contract parity 잔여 |
| original P0/P1 전체 | 약 75% | top-level TASK 39: Done 27, Partial/Review 3, Pending/Deferred/Blocked 9 |
| public/production readiness | 약 35~45% | auth/00700/remote deploy/ops/backup/performance/clean KPI 미완료 |

진행률은 LOC 비율이 아니라 user-visible acceptance와 운영 readiness를 가중한 공학적 추정이다.

## 6. 선택한 다음 순서

1. **Slice 0 — Git/docs baseline:** current docs/status branch review→필요 시 Draft PR→human merge.
2. **Slice 1 — local stabilization:** offices contract/runtime parity, quality-summary의 정직한
   deferred 경계, provider-disabled API/Web 5-scenario demo, manual a11y.
3. **Slice 2 — clean quality/ops:** B 승인 시 reset/reseed/19→20, quality schema/API,
   100-user smoke, backup/restore.
4. **Slice 3 — optional public:** `00700`, real admin auth, remote/deploy/CORS/secrets/logs/rollback.

P2 기능은 추가하지 않는다.

## 7. 변경 파일

| 파일 | 변경 |
|---|---|
| `TASKS.md` | 완료 상태를 actual PR/evidence에 맞게 갱신하고 OFFICE-API/CI-BACKEND gap 등록 |
| `docs/source-of-truth/PROJECT_PLAN.md` | Cloud/Frontend rehearsal 완료 상태 동기화 |
| `docs/handoffs/HANDOFF-20260726-CURRENT-MVP-STATUS.md` | current reproducible handoff 신규 |
| `docs/12_VERSIONING_AND_RELEASES.md` | documentation 2.20.4 근거 |
| `versions/manifest.json` | documentation 2.20.3→2.20.4 |
| 구현 노트·INDEX | status audit 기록 |
| 제품/API contract/DB/data/prompt/test | 변경 없음 |

## 8. 버전 전후

| 축 | Before | After |
|---|---|---|
| Product | 2.5.0 | 동일 |
| Application | 0.9.1-grounded-local-chat-evidence | 동일 |
| Web | 0.6.0-answer-mode | 동일 |
| API | 3.2.0-draft | 동일 |
| Shared contracts | 0.5.0 | 동일 |
| DB | 0.4.0-local | 동일 |
| Official data | 0.1.0-initial.2 | 동일 |
| Prompt | 0.2.0-grounded-live-chat | 동일 |
| Test | 1.6.1-grounded-actual | 동일 |
| Documentation | 2.20.3 | 2.20.4 |

## 9. 명령과 실제 결과

| 명령/검증 | 실제 결과 |
|---|---|
| `git fetch --prune origin` | PASS; `origin/main=be7387f` |
| `gh pr view 11`, `gh pr view 13` | 둘 다 MERGED |
| `gh pr list --state open` | open PR 0 |
| `gh run list --branch main` | current main policy/frontend SUCCESS |
| TASKS top-level parser | 39 total / Done 27 / Partial 3 / remaining 9 |
| FastAPI generated OpenAPI inventory | default health/ready/chat; local admin adds 7 admin paths; offices/quality-summary 없음 |
| `docker version`, `docker ps` | engine 29.2.1; local DB healthy |
| read-only aggregate SQL | ACTIVE 20 / offices 3 / mappings 10 |
| loopback health probes | API/Web NOT_RUNNING |
| `python -B scripts/check_repository_docs.py` | PASS — `repository documentation check passed` |
| repository secret scan | PASS — 발견 없음 |
| `python -m json.tool versions/manifest.json` | PASS |
| `git diff --check` | PASS |

### 미실행

- full API/Web/DB gate를 재실행하지 않았다. code/contract/DB를 변경하지 않은 status audit이며
  latest full publication gate와 current main CI 증거를 인용했다.
- API/Web server를 시작하지 않았다.
- provider actual, DB reset/seed/update/delete를 실행하지 않았다.

## 10. 보안·개인정보·데이터 영향

- read-only DB count만 확인했고 질문/event text는 조회하지 않았다.
- secret/DSN/key 값을 읽거나 출력하지 않았다.
- current event count는 D-077에 따라 KPI로 사용하지 않았다.
- official/mock data와 migration은 변경하지 않았다.
- UI/accessibility/runtime behavior 변화 없음.
- 외부 비용 0.

## 11. 인간이 반드시 알아야 할 내용

- local MVP는 “작동하는 데모” 기준으로 거의 완료됐지만 public 서비스는 아니다.
- 지금 바로 필요한 사람 작업은 manual a11y/demo와 collaborator MFA/recovery 확인이다.
- clean KPI가 필요하기 전에는 B reset을 하지 않는다.
- hosted backend CI는 Actions quota/비용과 범위를 결정해야 한다.
- public deploy가 불필요하면 Slice 3은 시작하지 않는다.

## 12. AI 내부 세부

- historical blocked DATA-SEED-001은 immutable `.2` successor 완료와 구분해 current blocker에서
  제거했다.
- manual a11y/demo를 WEB-CHAT 완료와 분리해 중복 미완료 계산을 줄였다.
- OpenAPI/runtime path set을 직접 비교해 parity gap을 backlog에 추가했다.

## 13. 재현·롤백·인수인계

### 재현

- handoff의 Git, DB, runtime, test evidence와 next acceptance를 순서대로 확인한다.
- latest remote 확인 없이 local `main`을 기준으로 새 branch를 만들지 않는다.

### 롤백

- 이번 docs/status commit만 revert한다.
- code/DB/provider rollback 없음.

### 인수인계

- 다음 개발자는 Slice 1의 offices parity와 provider-disabled local demo를 첫 작업으로 삼는다.
- quality-summary actual은 D-077/B gate와 contract 설계 전 구현하지 않는다.

## 14. 남은 위험

- 현재 DB event KPI 비권위
- hosted backend CI 부재
- offices/quality-summary contract/runtime gap
- manual accessibility/demo 증거 부족
- backup/performance/public security 미완료

## 15. 자체 리뷰

- [x] 사용자 질문에 범위별 진행률 제공
- [x] Git/PR/CI/runtime/DB evidence 확인
- [x] product/API/DB/data mutation 0
- [x] docs/secret/JSON/diff 최종 검증
- [x] handoff/implementation note/INDEX/versions 동기화
