# IMP-20260729-017 — 관리자 KB 후보 작성·승인 동작 확인

- Date/Time (KST): 2026-07-29
- Task ID: ADMIN-KB-STATUS-001
- Type/Status: decision-operations / Decision-only Done
- Branch/Base: `codex/local-demo-e2e-handoff` / `3c2343d`

## 사용자 요청과 인수 기준

- 요청: 현재 관리자 페이지에서 KB 후보 생성과 별도 승인 흐름이 실제로 작동하는지 확인한다.
- 기준: 구현 범위, 실행 전제, 자동 검증 증거와 미검증 상태를 구분하고 AI 자동 작성 여부를
  명확히 설명한다.

## 6W1H

| 항목 | 기록 |
|---|---|
| Who | 운영자 작성, 별도 승인자 검수, Codex가 코드·테스트 근거 확인 |
| When | 2026-07-29 KST |
| Where | local/private `/admin`, FastAPI admin service, local PostgreSQL |
| What | 실패 질문 → 사유 확정 → 후보 작성·제출 → 별도 승인 → ACTIVE 반영 |
| Why | 근거 부족 질문을 사람 검수형 공식 KB로 개선하기 위해 |
| How | 실제 Web/API/DB transport와 state-changing Playwright E2E 대조 |
| How much | 제품 변경 0, provider 호출 0, 문서 노트 1개와 INDEX 1행 |

## 조사 결과와 설계 경계

- `tools/web-e2e/e2e/admin-core-loop.actual.spec.ts`는 clean ACTIVE 19에서
  `INSUFFICIENT_GROUNDING` 질문을 저장하고 `/admin/failures`에서 사유를 확정한다.
- 운영자 `OPERATOR-LOCAL-001`이 공식 출처·요약·절차·수수료 등을 직접 입력해 후보를
  `PENDING_APPROVAL`로 제출한다.
- 승인자 역할로 전환해 검수 체크, 필수 검수 의견과 승인을 수행하면 서버가
  `KB-WASTE-03`을 ACTIVE로 반영한다.
- 같은 질문 재질의가 SUCCESS이며 서버 소유 공식 출처를 포함하는 것까지 E2E가 검증한다.
- 서비스·계약 테스트는 후보 작성자의 자기 승인 차단, 다른 승인자의 승인/반려, PII 포함
  후보와 비승인 출처 URL 차단을 검증한다.
- 현재 MVP의 `KB 후보 생성`은 **AI가 답안을 자동 작성하는 기능이 아니다.** 실패 질문과
  연결된 작성 폼을 열고 운영자가 공식 근거를 입력하는 흐름이다.
- 후보 자격은 `INSUFFICIENT_GROUNDING`만 갖는다. FOLLOWUP, NON_CIVIC,
  CIVIC_SCOPE_GAP, PERSONAL_LOOKUP, LEGAL_JUDGMENT는 이 실패 후보 흐름으로 들어가지 않는다.

## 명령·검증 증거

| 검증 | 결과 |
|---|---|
| actual Playwright spec의 사유 확정·후보 생성·제출·승인·ACTIVE·재질의 assertion 대조 | PASS |
| FastAPI admin service/repository/API wiring 대조 | PASS |
| 작성자 자기 승인 차단 및 별도 승인자 테스트 대조 | PASS |
| 현재 live browser 실행 | 미실행 — Docker/API/Web가 꺼진 상태이므로 사용자 후속 실행 Pending |

## 버전·데이터·보안 영향

- application/web/API/DB/official data/prompt/test version: 변경 없음.
- 공식 데이터와 immutable `.2`: 변경 없음. actual E2E 실행 시 disposable runtime만
  ACTIVE 19→20으로 변경된다.
- 질문 원문·PII·secret·provider body 저장 추가 없음. 실제 개인정보 대신 합성 질문만 사용한다.
- DeepSeek/Upstage는 후보 작성·승인 흐름의 필수 구성요소가 아니다.
- public 인증/RBAC/remote DB 기능이 아닌 local/private demo actor 흐름이다.

## 인간이 알아야 하는 내용

- `/ready=200`, Web `ADMIN_UI_MODE=actual`, local DB clean19가 실제 브라우저 E2E 전제다.
- exact 시연 질문은 `침대 2인용 프레임 수수료가 얼마예요?`이며 clean19에서만
  `INSUFFICIENT_GROUNDING` 후보가 된다. 승인 뒤 같은 질문은 SUCCESS가 된다.
- 이미 ACTIVE 20이면 이 exact 후보는 다시 만들지 말고 DB를 reset+immutable `.2` seed로
  clean19 복구해야 한다.
- 실제 기관 운영용 인증·권한 체계가 아니므로 public 배포에는 사용하지 않는다.

## AI 내부 구현 세부

- 새 코드·계약·migration·dependency 변경은 없다.
- 조사 근거는 actual E2E, admin service/API/repository와 관련 테스트다.

## 재현·롤백·다음 단계

- 재현: `IMP-20260729-016`의 actual E2E 절차를 clean19에서 정확히 한 번 실행한다.
- 롤백: 실제 E2E로 생성된 20번째 runtime KB는 disposable local DB reset과 immutable `.2`
  seed로 제거한다.
- 다음 단계: 사용자가 `/admin/failures`와 `/admin/approvals` 화면을 직접 확인하고 E2E PASS
  결과를 전달한다.

