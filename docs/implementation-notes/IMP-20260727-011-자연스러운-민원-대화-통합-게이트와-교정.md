# IMP-20260727-011 — 자연스러운 민원 대화 통합 게이트와 교정

- Date/Time (KST): 2026-07-27T04:35:49+09:00
- Task ID: CHAT-NATURAL-001-T14
- Type: testing-integration
- Status: Done
- Author/Agent: Codex `/root`
- Branch: codex/ACTUAL-P0-UX-GAPS-001
- Base commit: c9b9794
- Related plan/ADR/RFP: CHAT-NATURAL-001 plan Task 14, D-090~D-093, ADR-0010/0018/0024~0026

## 1. 사용자 요청과 완료 기준

### 요청

승인된 자연스러운 민원 대화 세 수직 흐름을 중단 없이 통합하고, 실제 DB·provider·public
단계로 가기 전 저장소 전체 gate를 통과시킨다.

### Acceptance Criteria

- API/Web/contracts/DB/root gate가 final tracked working tree에서 PASS한다.
- 공개 계약·버전·README와 generated type drift가 0이다.
- raw 질문·secret·DSN 출력 없이 DB runtime drift를 안전하게 복구한다.
- 발견된 결함은 focused RED/GREEN 뒤에만 수정한다.

## 2. 육하원칙(6W1H)

| 항목 | 기록 |
|---|---|
| Who — 누가 | 사용자 승인자, Codex 통합·보안·QA 담당 |
| When — 언제 | 2026-07-27 KST |
| Where — 어디서 | isolated worktree, API/Web/contracts/scripts/docs, loopback local PostgreSQL |
| What — 무엇을 | 전체 gate, active version drift, env fixture, Ruff formatting, test narrowing 교정 |
| Why — 왜 | actual DB/provider 전에 동일 source의 통합 가능성과 안전 경계를 증명하기 위해 |
| How — 어떻게 | fail-fast runner→direct reproduction→focused correction→constituent→root rerun |
| How much — 어느 정도 | API 2,137, contract 94, Web 60, DB pgTAP 385, root 433 cases |

## 3. 시작 전 상태

- 관련 파일: `scripts/verify.ps1`, DB runner, API/Web/contracts, current README/index/version docs.
- 기존 동작: area tests는 green이었지만 root gate가 active-version/env fixture/format/type narrowing
  drift를 순차적으로 발견했다.
- 발견한 충돌/부채: 실행 중 local DB가 승인 network 대신 Supabase 기본 network에 붙어 있었다.
- Git 상태: 격리 worktree, primary checkout의 사용자 변경 미접촉.

## 4. 미지의 영역·가정·인터뷰

| ID | 구분 | 내용 | 결정/기본값 | 영향 |
|---|---|---|---|---|
| D-092 | 인간 승인 | local DB reset·seed와 실제 provider | 승인 범위 안에서 bounded 실행 | DB·비용 |
| Remote target | discovery | provider/project/domain 설정 여부 | 설정 없으면 임의 생성 없이 Not executed | public |

## 5. 설계 결정과 대안

### 선택

각 실패를 runner가 숨긴 child output과 분리해 같은 명령을 직접 재현하고, 문서 drift·보안 fixture·
formatter·test Optional narrowing만 최소 교정했다.

### 이유

gate 우회나 test 삭제 없이 실제 원인을 고치고, 후속 actual 증거가 신뢰할 수 있는 기준선을
만든다.

### 고려했지만 선택하지 않은 대안

- root 실패를 area green으로 대체: orchestration 결함을 숨기므로 제외.
- 기본-network DB를 그대로 사용: loopback network identity gate 위반.
- formatter check 비활성화: 일관성과 CI parity를 깨므로 제외.

## 6. 구현 상세

| 파일/영역 | 변경 내용 | 이유 |
|---|---|---|
| README/API 계약 문서/index | API 4.0, context v2, DB 11/385 현재값 정합 | scaffold drift 제거 |
| security boundary test | 승인된 classifier env 8개 exact default 추가 | env allowlist 정합 |
| API source/tests | Ruff formatter의 기계적 정리 | root FORMAT-API |
| chat service test | fallback 존재 assert 뒤 reason 접근 | mypy strict narrowing |
| integration report | 명령·결과·복구·잔여 경계 기록 | 재현 가능한 증거 |

### 데이터 흐름/상태 변화

공식 data file과 application row는 변경하지 않았다. DB gate는 승인된 disposable local runtime을
reset해 migration/rollback/pgTAP을 재검증했으며 formal `.2` import는 다음 Task 15가 소유한다.

### 오류·빈 상태·롤백

기본 network runtime은 exact project label 하나임을 확인한 뒤 patched CLI `stop`으로 제거하고
safe network에서 runner가 재생성했다. 교정 파일은 해당 통합 commit revert로 롤백할 수 있다.

## 7. 버전 전후

| 축 | Before | After | 변경 이유 |
|---|---|---|---|
| Application | 0.11.0-natural-dialogue | 동일 | 검증/교정 |
| Web | 0.7.0-natural-dialogue | 동일 | 검증 |
| API | 4.0.0-draft | 동일 | active 문서 정합 |
| DB schema | 0.5.0-local | 동일 | reset/replay only |
| Official data | 0.1.0-initial.2 | 동일 | formal import 전 |
| Mock data | 0.0.0-not-populated | 동일 | 변경 없음 |
| Prompt set | 0.3.0-hybrid-classifier | 동일 | actual 전 |
| Test suite | 1.9.0-natural-dialogue | 동일 | regression fixture 정합 |
| Docs | 2.24.0 | 동일 | patch 정합 |

## 8. 명령과 테스트 증거

| 명령/검증 | 결과 | 시간/개수 | 증거 경로 |
|---|---|---|---|
| API pytest | PASS | 2,137 passed, 8 DB-only skipped, 5 subtests | integration report |
| Ruff format/check + mypy | PASS | 110 files, 0 issue | integration report |
| shared contracts | PASS | 94/94, generated drift 0 | integration report |
| Web test/lint/typecheck/build | PASS | 60/60, production build | integration report |
| DB verifier | PASS | 52.8s, 11 pgTAP files/385 tests, 11-stage replay | integration report |
| root verifier | PASS | 644.5s, 433 discovered root cases and all stages | integration report |
| docs/package/secret/diff | PASS | leak finding 0 | integration report |

### 미실행 검증과 이유

actual `.2` 19→20, provider aggregate, remote target smoke는 각각 Task 15~17에서 상태·비용 경계를
분리해 실행한다.

## 9. 보안·개인정보·접근성·성능 영향

- Privacy: raw 질문·provider payload·DB row·DSN 출력 0.
- Security: default Docker network drift를 거부하고 exact loopback runtime만 허용했다.
- Accessibility: 기존 Web unit/build green; 실제 followup E2E는 Slice 2 증거를 보존한다.
- Performance/cost: 이 단계 외부 provider 호출 0, 비용 USD 0.

## 10. 데이터와 출처 영향

- 공식 데이터: immutable `.2` bytes 및 version 변경 0.
- mock/AI 생성: 새 데이터 0.
- schema/lineage: 00100~00700 reset/rollback/reapply만 수행.
- verified date: 2026-07-27.

## 11. 인간이 반드시 알아야 하거나 승인할 내용

- root gate는 최종 PASS지만 actual seed/provider/remote 완료를 뜻하지 않는다.
- local DB는 disposable 승인 범위에서 reset됐다.

## 12. AI 내부 구현 세부 — 필요할 때만 보면 되는 내용

- pinned `uv.exe`는 ignored `.tools`에 hash-identical로 복제해 worktree runner가 사용했다.
- test fixture와 formatter 변경은 공개 행동을 바꾸지 않는다.

## 13. 인수인계·재현·롤백

### 재현

`scripts/verify_database.ps1` 뒤 `scripts/verify.ps1`을 Windows PowerShell에서 실행한다.

### 롤백

이 노트와 report 및 integration correction commit을 revert한다. DB는 00700부터 newest-first
rollback하거나 supported full reset으로 복구한다.

### 다음 개발자 시작점

Task 15의 `verify_data_seed.ps1 -ReleaseVersion 0.1.0-initial.2`와
`verify_actual_mvp_regression.py`를 실행한다.

## 14. 남은 위험·미해결 질문·다음 단계

- actual provider 품질/비용과 remote target 존재 여부는 아직 이 단계의 완료 주장 밖이다.

## 15. 자체 리뷰

- [x] 요청 충족
- [x] 테스트/검증
- [x] source-of-truth/계약/버전 동기화
- [x] 개인정보 원문 노출 없음
- [x] 구현 노트 INDEX 갱신
