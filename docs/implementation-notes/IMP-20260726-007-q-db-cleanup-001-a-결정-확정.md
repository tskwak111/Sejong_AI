# IMP-20260726-007 — Q-DB-CLEANUP-001 A 결정 확정

- Date/Time (KST): 2026-07-26T14:09:33+09:00
- Task ID: Q-DB-CLEANUP-001
- Type: decision
- Status: Decision Done
- Author/Agent: 사용자 / Codex 기록
- Branch: codex/LLM-003-metadata-explanation
- Base commit: 9678ebd
- Related:
  - [결정 설명](IMP-20260726-006-오표시-metadata-22행과-cleanup-선택지-설명.md)
  - [Decision Log](../decisions/DECISION_LOG.md)
  - [Ambiguity Register](../11_AMBIGUITY_REGISTER.md)

## 1. 사용자 요청과 완료 기준

### 요청

사용자는 다음 운영 결정을 확정했다.

> 지금은 A로 유지하고 개발을 계속한다. 현재 local DB의 event 통계는 평가 KPI로
> 사용하지 않는다. 정식 수치가 필요해지는 시점에 B를 승인받아 reset·재시드·승인 흐름을
> 재현한다.

### Acceptance Criteria

- Q-DB-CLEANUP-001=A를 새 결정 ID로 기록한다.
- source-of-truth·결정 로그·모호성 등록부·버전 문서를 같은 의미로 동기화한다.
- 현재 DB 22행의 delete/update/reset과 외부 provider 호출은 수행하지 않는다.
- B의 future approval gate와 재현 범위를 명시한다.
- 구현 노트와 INDEX를 갱신한다.

## 2. 육하원칙(6W1H)

| 항목 | 기록 |
|---|---|
| Who — 누가 | 사용자가 A를 최종 결정했고 Codex가 D-077/A-050으로 기록했다. |
| When — 언제 | 2026-07-26 14:09 KST |
| Where — 어디서 | local/private source-of-truth·decision·ambiguity·version 문서 |
| What — 무엇을 | 오표시 22행 유지, 현재 event KPI 비권위, clean KPI 전 B 재승인 경계를 확정했다. |
| Why — 왜 | 식별자 없는 선별 삭제로 정상 local 상태를 훼손하지 않으면서 개발을 계속하기 위해서다. |
| How — 어떻게 | 문서만 동기화하고 DB·provider·제품 동작은 건드리지 않았다. |
| How much — 어느 정도 | 결정 D-077 1건, ambiguity A-050 1건, documentation 2.20.2→2.20.3; DB mutation·provider call 0 |

## 3. 시작 전 상태

- PR #13은 병합됐고 current main evidence SHA는 `be7387f`다.
- 두 local actual run에서 metadata-only `interaction_events` 22행이
  `is_test=false`로 오표시됐다.
- 현재 runner는 future actual write를 `is_test=true`로 강제한다.
- 정확한 22개 request ID/run marker는 tracked evidence에 없다.
- Q-DB-CLEANUP-001은 설명됐지만 인간 선택이 아직 decision log에 확정되지 않은 상태였다.
- 시작 HEAD `9678ebd`, 시작 worktree clean.

## 4. 미지의 영역·가정·인터뷰

| ID | 구분 | 내용 | 결정/기본값 | 영향 |
|---|---|---|---|---|
| Q-DB-CLEANUP-001 | Human | 지금 22행을 유지할지 reset할지 | A / D-077 확정 | local DB·통계 신뢰성 |
| A-050 | Resolved | cleanup 운영 경계 | 22행 유지, current local event KPI 비권위 | 평가 보고 |
| Future B | Human gate | 언제 reset·재시드·승인 흐름을 재현할지 | 정식 KPI/benchmark 필요 시 별도 승인 | DB 삭제·시연 상태 재현 |

## 5. 설계 결정과 대안

### 선택

- A를 현재 상태로 확정한다.
- 22행은 유지하고 delete/update/reset하지 않는다.
- 현재 local DB snapshot의 event 통계는 평가 KPI로 인용하지 않는다.
- 기능 개발은 이 문제 때문에 중단하지 않는다.
- 정식 수치가 필요해질 때 B를 별도 승인받는다.

### B 승인 시 재현 경계

1. disposable local DB reset 범위와 보존할 증거 확인
2. versioned migration 재적용
3. immutable official `.2` seed 실행
4. 19 ACTIVE·office 3·mapping 10 검증
5. 필요하면 실패 질문→후보→별도 승인자→20번째 ACTIVE 흐름 재현
6. clean state에서 새 benchmark 실행

### 선택하지 않은 대안

- 현재 즉시 B: 개발을 막을 정도의 보안·개인정보 사고가 아니며 mutable demo 상태 재현
  비용이 생겨 선택하지 않았다.
- targeted delete/update: 정확한 ID set이 없어 정상 local event 훼손 가능성이 있으므로
  선택하지 않았다.

## 6. 변경 상세

| 파일/영역 | 변경 내용 | 이유 |
|---|---|---|
| `docs/decisions/DECISION_LOG.md` | D-077 추가와 actual evidence 설명 동기화 | 인간 결정 권위 |
| `docs/11_AMBIGUITY_REGISTER.md` | A-050 Resolved 추가 | 재질문 방지 |
| `docs/00_SOURCE_OF_TRUTH.md` | A 유지·KPI 비권위·future B gate 반영 | 최상위 현재 상태 |
| `docs/source-of-truth/TEAM_DECISIONS.md` | local actual 및 기술 상태 문구 동기화 | 중복 사실 충돌 방지 |
| `docs/12_VERSIONING_AND_RELEASES.md` | documentation 2.20.3 근거 추가 | 버전 계보 |
| `versions/manifest.json` | documentation 2.20.2→2.20.3 | current manifest |
| 구현 노트·INDEX | 결정 재현·색인 추가 | 작업 완료 기준 |
| 제품/API/DB/data/prompt/test | 변경 없음 | 운영 결정 기록만 수행 |

### 데이터·상태 변화

- tracked documentation만 변경했다.
- local DB를 조회·reset·delete·update하지 않았다.
- Upstage API를 호출하지 않았다.
- 22행, official KB, 19→20 ACTIVE, 실패 질문·후보 상태는 모두 그대로다.

## 7. 버전 전후

| 축 | Before | After | 변경 이유 |
|---|---|---|---|
| Product spec | 2.5.0 | 동일 | 제품 범위 불변 |
| Application | 0.9.1-grounded-local-chat-evidence | 동일 | 코드 불변 |
| Web | 0.6.0-answer-mode | 동일 | 코드 불변 |
| API | 3.2.0-draft | 동일 | 계약 불변 |
| Shared contracts | 0.5.0 | 동일 | 계약 불변 |
| DB schema | 0.4.0-local | 동일 | migration/data mutation 없음 |
| Official data | 0.1.0-initial.2 | 동일 | seed 불변 |
| Mock data | 0.0.0-not-populated | 동일 | mock 불변 |
| Prompt set | 0.2.0-grounded-live-chat | 동일 | provider/prompt 불변 |
| Test suite | 1.6.1-grounded-actual | 동일 | 테스트 코드 불변 |
| Documentation | 2.20.2 | 2.20.3 | D-077/A-050 기록 |

## 8. 명령과 테스트 증거

| 명령/검증 | 결과 | 증거 |
|---|---|---|
| `python scripts/new_implementation_note.py ...` | PASS | 이 노트와 INDEX row 생성 |
| `python -B scripts/check_repository_docs.py` | PASS — `repository documentation check passed` | 문서 링크·권위 검사 |
| `powershell.exe ... scripts/check_secret_patterns.ps1 ...` | PASS — 발견 없음 | 비밀 패턴 검사 |
| `python -m json.tool versions/manifest.json` | PASS — `manifest JSON valid` | manifest JSON 검증 |
| `git diff --check` | PASS | whitespace 검사 |

### 미실행 검증

- API/Web/DB 테스트와 build는 제품·계약·DB 변경이 없어 실행하지 않는다.
- local DB actual, seed, reset, provider actual은 A 결정에 따라 실행하지 않는다.

## 9. 보안·개인정보·접근성·성능 영향

- 개인정보: 질문 원문·마스킹 질문·답변·provider payload를 읽거나 저장하지 않았다.
- 보안: secret/DSN/API key를 사용하지 않았다. public/remote 권한은 넓어지지 않았다.
- 데이터 품질: 현재 local event 통계는 평가 KPI가 아니라는 제한을 명시했다.
- 접근성/성능: UI·runtime 변경 없음.
- 비용: 외부 API 호출 0원.

## 10. 데이터와 출처 영향

- official/mock data 변경 없음.
- DB schema·row·lineage 변경 없음.
- historical 22행의 의미만 D-077/A-050으로 분류했다.
- 확인 기준: 2026-07-26 KST, main evidence `be7387f`, 결정 branch base `9678ebd`.

## 11. 인간이 반드시 알아야 하거나 승인할 내용

- A는 확정됐으므로 같은 질문을 다시 묻지 않는다.
- 현재 local event count를 데모 성과·평가 KPI로 제시하면 안 된다.
- clean KPI가 필요해지면 B를 새로 승인해야 한다.
- B 승인은 DB reset과 정식 seed/replay 범위를 승인하는 것이며 future provider actual은
  별도 승인이다.
- 이번 결정은 public deploy, remote DB, 자동 merge를 승인하지 않는다.

## 12. AI 내부 구현 세부

- D-077/A-050 ID를 사용해 결정과 ambiguity를 연결했다.
- ADR은 만들지 않았다. architecture/schema/contract를 바꾸지 않는 local data-quality
  운영 결정이기 때문이다.
- documentation patch만 올리고 나머지 manifest 축은 유지했다.

## 13. 인수인계·재현·롤백

### 재현

1. D-077에서 A의 정확한 경계를 확인한다.
2. A-050이 Resolved인지 확인한다.
3. source-of-truth 두 문서가 event KPI 비권위와 future B gate를 동일하게 말하는지 확인한다.
4. manifest와 versioning 문서가 `2.20.3`인지 확인한다.

### 롤백

- 이 문서 commit을 revert하면 된다.
- DB·provider·제품 rollback은 없다.
- 인간 결정 자체를 바꾸려면 기존 D-077을 삭제하지 않고 새 decision row로 supersede한다.

### 다음 개발자 시작점

- event KPI를 인수 기준이나 발표 수치로 사용하지 않는 기능 개발은 계속할 수 있다.
- KPI task 시작 시 D-077을 확인하고 B 승인 여부부터 판정한다.

## 14. 남은 위험·다음 단계

- 현재 local DB는 기능 시연에는 쓸 수 있지만 clean event benchmark source는 아니다.
- 향후 exact cleanup marker를 설계하려면 별도 architecture/data-model 검토가 필요하다.
- 다음 기능 개발은 승인된 범위 안에서 진행 가능하다.

## 15. 자체 리뷰

- [x] 요청 충족
- [x] 문서·JSON·secret·diff 검증
- [x] source-of-truth/결정/ambiguity/버전 동기화
- [x] DB·provider mutation 0
- [x] 개인정보 원문 노출 없음
- [x] 구현 노트 INDEX 갱신
