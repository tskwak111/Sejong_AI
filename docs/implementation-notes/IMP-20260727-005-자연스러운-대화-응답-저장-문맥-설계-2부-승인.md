# IMP-20260727-005 — 자연스러운 대화 응답 저장 문맥 설계 2부 승인

- Date/Time (KST): 2026-07-27T00:50:05+09:00
- Task ID: `CHAT-NATURAL-DESIGN-SECTION-2`
- Type: decision-design
- Status: Decision-only Done / design section 3 pending
- Author/Agent: 사용자 승인 / Codex 기록
- Branch: `codex/ACTUAL-P0-UX-GAPS-001`
- Base commit: `bcc5810`
- Related: D-090, ADR-0010/0024, A-014/A-059, CHAT-NATURAL-001

## 1. 요청과 완료 기준

사용자가 설계 2부를 승인했다. route별 시민 응답·저장과 context v2 경계를 권위 문서와
ADR에 기록하고, exact 계약/DB/runtime 구현은 전체 설계·명세·계획 승인 전 보류한다.

## 2. 6W1H

| 항목 | 기록 |
|---|---|
| Who | 사용자 승인, Codex 문서화 |
| When | 2026-07-27 KST |
| Where | SOT, ADR-0010/0024, decision/ambiguity/TASKS/version |
| What | CIVIC_SCOPE_GAP 공개 reason·별도 queue, 무저장 route, context v2 |
| Why | 자연스러운 시민 동작과 개인정보·운영 경계를 동시에 고정 |
| How | D-090과 ADR amendment |
| How much | product/API/DB/data/provider actual 0 |

## 3. 시작 상태와 결정

- 설계 1부의 privacy-first hybrid pipeline은 D-089로 승인됐다.
- Q-SCOPE-001=A의 별도 queue 방향은 있었지만 public reason과 context claims는 미확정이었다.
- 시작 HEAD `bcc5810`, worktree clean.

승인 내용:

- `CIVIC_SCOPE_GAP`: public `intent=OUT_OF_SCOPE`, reason `CIVIC_SCOPE_GAP`, candidate false,
  별도 queue masked text 30일, 기존 event/failed/candidate와 분리, 자동 ACTIVE 0
- NON_CIVIC/PERSONAL/LEGAL/PRIVACY: text/event/failed/review row 0
- FOLLOWUP: failure/scope row 0
- context v2: closed topic ID, pending slot, dialog act, region/status/time/server option
- v1은 남은 최대 TTL read-only, issuer v2 only
- topic은 매 요청 ACTIVE/OFFICIAL 재검증, Web 새 대화는 transcript/token 초기화

## 4. 미결정

| 영역 | 상태 | 다음 단계 |
|---|---|---|
| exact OpenAPI/Pydantic/TS enum | Approved meaning / unimplemented | written spec |
| table/status/function/endpoint/migration 번호 | Pending detail | written spec |
| 오류·latency·test acceptance | Pending section 3 | 다음 사용자 검토 |

## 5. 변경 파일과 버전

| 영역 | 변경 |
|---|---|
| SOT/TEAM_DECISIONS | 승인된 공개·저장·context 경계 |
| ADR-0010/0024 | v2·scope queue amendment |
| decision/ambiguity/TASKS | D-090, A-014/A-059, 진행 상태 |
| changelog/version/note/index | docs `2.22.5→2.22.6` |

Application `0.10.0`, Web `0.6.0`, API `3.3.0-draft`, DB `0.4.0-local`, official data
`0.1.0-initial.2`, prompt `0.2.0`, tests `1.8.0`은 그대로다.

## 6. 명령·검증

| 검증 | 결과 |
|---|---|
| 시작 `git status`, `git log -1` | clean / `bcc5810` |
| `python -B scripts/check_repository_docs.py` | PASS — `repository documentation check passed` |
| `scripts/check_secret_patterns.ps1 -RepositoryRoot .` | PASS / findings 0 |
| `git diff --check` | PASS |
| `python -m json.tool versions/manifest.json` | PASS |

제품·test source가 바뀌지 않아 API/Web/DB test/build는 실행하지 않는다. provider/DB call,
seed/reset/migration도 0이다.

## 7. 영향과 경계

- Privacy: 원문은 어느 route에도 저장하지 않는다. scope-gap masked text만 새 목적 30일이다.
- Security: token은 auth가 아니며 topic도 authority가 아니다.
- Accessibility: 새 대화 control의 키보드/focus는 future spec 대상이다.
- Performance/cost: actual call 0, Q-CLASS-002 cap 유지.
- Data/source: record/schema/official/mock 변경 0.

## 8. 인간/AI 책임

인간은 public enum, masked scope 보관, v2 claims를 승인했다. exact migration/API와 실제
공개 운영의 보관·인증·배포는 후속 승인 대상이다. helper/validator/test fixture 분리는 최종
계약 안에서 AI가 자율 처리할 수 있다.

## 9. 롤백·인수인계·다음 단계

이 documentation commit을 revert하면 된다. DB/data rollback은 없다. 다음 개발자는 D-090과
두 ADR을 읽고 설계 3부의 오류·fallback·latency·test acceptance를 검토한다.

## 10. 자체 리뷰

- [x] 사용자 승인과 ADR/SOT 반영
- [x] final 문서/secret/diff 검증
- [x] 버전·INDEX 동기화
- [x] 시민 원문·비밀 없음
